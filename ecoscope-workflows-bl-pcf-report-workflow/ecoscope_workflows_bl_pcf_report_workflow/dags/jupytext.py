# AUTOGENERATED BY ECOSCOPE-WORKFLOWS; see fingerprint in README.md for details


# ruff: noqa: E402

# %% [markdown]
# # Bl Pcf Report
# TODO: top level description

# %% [markdown]
# ## Imports

import os

from ecoscope_workflows_core.tasks.config import (
    set_workflow_details as set_workflow_details,
)
from ecoscope_workflows_core.tasks.filter import set_time_range as set_time_range
from ecoscope_workflows_core.tasks.groupby import set_groupers as set_groupers
from ecoscope_workflows_core.tasks.groupby import split_groups as split_groups
from ecoscope_workflows_core.tasks.io import persist_text as persist_text
from ecoscope_workflows_core.tasks.io import set_er_connection as set_er_connection
from ecoscope_workflows_core.tasks.results import gather_dashboard as gather_dashboard
from ecoscope_workflows_core.tasks.skip import (
    any_dependency_skipped as any_dependency_skipped,
)
from ecoscope_workflows_core.tasks.skip import any_is_empty_df as any_is_empty_df
from ecoscope_workflows_core.tasks.transformation import filter_df as filter_df
from ecoscope_workflows_core.tasks.transformation import map_columns as map_columns
from ecoscope_workflows_core.tasks.transformation import sort_values as sort_values
from ecoscope_workflows_ext_big_life.tasks import aggregate_by as aggregate_by
from ecoscope_workflows_ext_big_life.tasks import combine_columns as combine_columns
from ecoscope_workflows_ext_big_life.tasks import (
    convert_numeric_to_int as convert_numeric_to_int,
)
from ecoscope_workflows_ext_big_life.tasks import crosstab_summary as crosstab_summary
from ecoscope_workflows_ext_big_life.tasks import (
    draw_custom_100_stacked_bar_chart as draw_custom_100_stacked_bar_chart,
)
from ecoscope_workflows_ext_big_life.tasks import (
    draw_custom_bar_chart as draw_custom_bar_chart,
)
from ecoscope_workflows_ext_big_life.tasks import (
    draw_custom_multi_bar_time_series as draw_custom_multi_bar_time_series,
)
from ecoscope_workflows_ext_big_life.tasks import (
    draw_custom_multi_line_time_series as draw_custom_multi_line_time_series,
)
from ecoscope_workflows_ext_big_life.tasks import (
    draw_custom_stacked_bar_chart as draw_custom_stacked_bar_chart,
)
from ecoscope_workflows_ext_big_life.tasks import (
    draw_historic_time_series_chart as draw_historic_time_series_chart,
)
from ecoscope_workflows_ext_big_life.tasks import (
    format_numeric_with_commas as format_numeric_with_commas,
)
from ecoscope_workflows_ext_big_life.tasks import (
    generate_density_grid as generate_density_grid,
)
from ecoscope_workflows_ext_big_life.tasks import (
    generate_pcf_report as generate_pcf_report,
)
from ecoscope_workflows_ext_big_life.tasks import get_color_map as get_color_map
from ecoscope_workflows_ext_big_life.tasks import (
    get_user_full_name as get_user_full_name,
)
from ecoscope_workflows_ext_big_life.tasks import (
    map_color_column_value as map_color_column_value,
)
from ecoscope_workflows_ext_big_life.tasks import reorder_cols as reorder_cols
from ecoscope_workflows_ext_big_life.tasks import (
    select_time_frequency as select_time_frequency,
)
from ecoscope_workflows_ext_big_life.tasks import (
    shift_previous_period as shift_previous_period,
)
from ecoscope_workflows_ext_big_life.tasks import (
    summarize_predation_table as summarize_predation_table,
)
from ecoscope_workflows_ext_big_life.tasks import to_percent as to_percent
from ecoscope_workflows_ext_custom.tasks.io import get_current_user as get_current_user
from ecoscope_workflows_ext_custom.tasks.io import html_to_png as html_to_png
from ecoscope_workflows_ext_custom.tasks.io import load_df as load_df
from ecoscope_workflows_ext_custom.tasks.io import (
    process_events_details as process_events_details,
)
from ecoscope_workflows_ext_custom.tasks.results import (
    create_geojson_layer as create_geojson_layer,
)
from ecoscope_workflows_ext_custom.tasks.results import (
    create_scatterplot_layer as create_scatterplot_layer,
)
from ecoscope_workflows_ext_custom.tasks.results import draw_map as draw_map
from ecoscope_workflows_ext_custom.tasks.results import (
    set_base_maps_pydeck as set_base_maps_pydeck,
)
from ecoscope_workflows_ext_custom.tasks.spatial_ops import (
    reproject_gdf as reproject_gdf,
)
from ecoscope_workflows_ext_custom.tasks.transformation import (
    drop_column_prefix as drop_column_prefix,
)
from ecoscope_workflows_ext_custom.tasks.transformation import (
    drop_null_geometry as drop_null_geometry,
)
from ecoscope_workflows_ext_custom.tasks.transformation import (
    filter_row_values as filter_row_values,
)
from ecoscope_workflows_ext_custom.tasks.transformation import (
    merge_two_dataframes as merge_two_dataframes,
)
from ecoscope_workflows_ext_ecoscope.tasks.io import get_events as get_events
from ecoscope_workflows_ext_ecoscope.tasks.io import persist_df as persist_df
from ecoscope_workflows_ext_ecoscope.tasks.results import (
    draw_pie_chart as draw_pie_chart,
)
from ecoscope_workflows_ext_ecoscope.tasks.transformation import (
    apply_classification as apply_classification,
)
from ecoscope_workflows_ext_ecoscope.tasks.transformation import (
    apply_color_map as apply_color_map,
)
from ecoscope_workflows_ext_ecoscope.tasks.transformation import (
    normalize_json_column as normalize_json_column,
)
from ecoscope_workflows_ext_mnc.tasks import convert_to_int as convert_to_int
from ecoscope_workflows_ext_mnc.tasks import (
    exclude_geom_outliers as exclude_geom_outliers,
)
from ecoscope_workflows_ext_mnc.tasks import (
    replace_missing_with_label as replace_missing_with_label,
)
from ecoscope_workflows_ext_ste.tasks import (
    annotate_gdf_dict_with_geom_type as annotate_gdf_dict_with_geom_type,
)
from ecoscope_workflows_ext_ste.tasks import (
    combine_deckgl_map_layers as combine_deckgl_map_layers,
)
from ecoscope_workflows_ext_ste.tasks import (
    create_deckgl_layers_from_gdf_dict as create_deckgl_layers_from_gdf_dict,
)
from ecoscope_workflows_ext_ste.tasks import (
    dataframe_column_first_unique_str as dataframe_column_first_unique_str,
)
from ecoscope_workflows_ext_ste.tasks import (
    fetch_and_persist_file as fetch_and_persist_file,
)
from ecoscope_workflows_ext_ste.tasks import split_gdf_by_column as split_gdf_by_column
from ecoscope_workflows_ext_ste.tasks import view_state_deck_gdf as view_state_deck_gdf
from ecoscope_workflows_ext_ste.tasks import zip_groupbykey as zip_groupbykey

# %% [markdown]
# ## Set Workflow Details

# %%
# parameters

workflow_details_params = dict(
    name=...,
    description=...,
    image_url=...,
)

# %%
# call the task


workflow_details = (
    set_workflow_details.set_task_instance_id("workflow_details")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(**workflow_details_params)
    .call()
)


# %% [markdown]
# ## Set Report Time Range

# %%
# parameters

time_range_params = dict(
    since=...,
    until=...,
    timezone=...,
    time_format=...,
)

# %%
# call the task


time_range = (
    set_time_range.set_task_instance_id("time_range")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(**time_range_params)
    .call()
)


# %% [markdown]
# ## Set Data Grouping

# %%
# parameters

groupers_params = dict()

# %%
# call the task


groupers = (
    set_groupers.set_task_instance_id("groupers")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(groupers=[{"index_name": "Ranch"}], **groupers_params)
    .call()
)


# %% [markdown]
# ## Connect to EarthRanger

# %%
# parameters

er_client_name_params = dict(
    data_source=...,
)

# %%
# call the task


er_client_name = (
    set_er_connection.set_task_instance_id("er_client_name")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(**er_client_name_params)
    .call()
)


# %% [markdown]
# ## Configure base map layers

# %%
# parameters

configure_base_maps_params = dict()

# %%
# call the task


configure_base_maps = (
    set_base_maps_pydeck.set_task_instance_id("configure_base_maps")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        base_maps=[
            {
                "url": "https://server.arcgisonline.com/arcgis/rest/services/Elevation/World_Hillshade/MapServer/tile/{z}/{y}/{x}",
                "opacity": 1,
                "max_zoom": 20,
            },
            {
                "url": "https://server.arcgisonline.com/ArcGIS/rest/services/World_Street_Map/MapServer/tile/{z}/{y}/{x}",
                "opacity": 0.15,
                "max_zoom": 20,
            },
        ],
        **configure_base_maps_params,
    )
    .call()
)


# %% [markdown]
# ## Download amboseli conservancy layers

# %%
# parameters

ambo_ranch_layers_params = dict()

# %%
# call the task


ambo_ranch_layers = (
    fetch_and_persist_file.set_task_instance_id("ambo_ranch_layers")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        url="https://www.dropbox.com/scl/fi/wrcp3pnnxqyrclzmbtt1r/amboseli_ranch_conservancies_layers.gpkg?rlkey=jfbqj1b1nwhijxbsy7b2v1d29&st=d7rbqdfe&dl=0",
        output_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        overwrite_existing=False,
        retries=3,
        unzip=False,
        **ambo_ranch_layers_params,
    )
    .call()
)


# %% [markdown]
# ## Download amboseli ranch boundaries and electric fence

# %%
# parameters

ambo_bound_fence_params = dict()

# %%
# call the task


ambo_bound_fence = (
    fetch_and_persist_file.set_task_instance_id("ambo_bound_fence")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        url="https://www.dropbox.com/scl/fi/k6dgrmxmxeb3fe6glihwa/amboseli_group_ranch_boundaries_x_electric_fence.gpkg?rlkey=limtzffe88lr0iof5kf3b6uoy&st=rreho3tq&dl=0",
        output_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        overwrite_existing=False,
        retries=3,
        unzip=False,
        **ambo_bound_fence_params,
    )
    .call()
)


# %% [markdown]
# ## Download Amboseli Conservancies

# %%
# parameters

ambo_conservancies_params = dict()

# %%
# call the task


ambo_conservancies = (
    fetch_and_persist_file.set_task_instance_id("ambo_conservancies")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        url="https://www.dropbox.com/scl/fi/blo1smf49buhwx24y4z60/amboseli_group_ranch_boundaries.gpkg?rlkey=w86emavj8ug7zab73ifbzg6jv&st=v8uptlb0&dl=0",
        output_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        overwrite_existing=False,
        retries=3,
        unzip=False,
        **ambo_conservancies_params,
    )
    .call()
)


# %% [markdown]
# ## Select time frequency for temporal charts

# %%
# parameters

time_frequency_params = dict(
    time_frequency=...,
)

# %%
# call the task


time_frequency = (
    select_time_frequency.set_task_instance_id("time_frequency")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(**time_frequency_params)
    .call()
)


# %% [markdown]
# ## Load amboseli conservancy layers

# %%
# parameters

load_ambo_layers_params = dict()

# %%
# call the task


load_ambo_layers = (
    load_df.set_task_instance_id("load_ambo_layers")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        file_path=ambo_ranch_layers,
        layer=None,
        deserialize_json=False,
        **load_ambo_layers_params,
    )
    .call()
)


# %% [markdown]
# ## Load amboseli ranch boundaries and electric fence

# %%
# parameters

load_ambo_boundaries_fence_params = dict()

# %%
# call the task


load_ambo_boundaries_fence = (
    load_df.set_task_instance_id("load_ambo_boundaries_fence")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        file_path=ambo_bound_fence,
        layer=None,
        deserialize_json=False,
        **load_ambo_boundaries_fence_params,
    )
    .call()
)


# %% [markdown]
# ## Load amboseli conservancies

# %%
# parameters

load_ambo_conservancies_params = dict()

# %%
# call the task


load_ambo_conservancies = (
    load_df.set_task_instance_id("load_ambo_conservancies")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        file_path=ambo_conservancies,
        layer=None,
        deserialize_json=False,
        **load_ambo_conservancies_params,
    )
    .call()
)


# %% [markdown]
# ## Reproject Amboseli Conservancy Layers to WGS84

# %%
# parameters

reproject_ambo_layers_params = dict()

# %%
# call the task


reproject_ambo_layers = (
    reproject_gdf.set_task_instance_id("reproject_ambo_layers")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        gdf=load_ambo_layers, target_crs="EPSG:4326", **reproject_ambo_layers_params
    )
    .call()
)


# %% [markdown]
# ## Reproject Amboseli Ranch Boundaries and Electric Fence to WGS84

# %%
# parameters

reproject_ambo_boundaries_params = dict()

# %%
# call the task


reproject_ambo_boundaries = (
    reproject_gdf.set_task_instance_id("reproject_ambo_boundaries")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        gdf=load_ambo_boundaries_fence,
        target_crs="EPSG:4326",
        **reproject_ambo_boundaries_params,
    )
    .call()
)


# %% [markdown]
# ## Reproject Amboseli Conservancies to WGS84

# %%
# parameters

reproject_ambo_conservancies_params = dict()

# %%
# call the task


reproject_ambo_conservancies = (
    reproject_gdf.set_task_instance_id("reproject_ambo_conservancies")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        gdf=load_ambo_conservancies,
        target_crs="EPSG:4326",
        **reproject_ambo_conservancies_params,
    )
    .call()
)


# %% [markdown]
# ## Split Amboseli Layers by Land Use

# %%
# parameters

split_ambo_layers_use_params = dict()

# %%
# call the task


split_ambo_layers_use = (
    split_gdf_by_column.set_task_instance_id("split_ambo_layers_use")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        gdf=reproject_ambo_layers, column="land_use", **split_ambo_layers_use_params
    )
    .call()
)


# %% [markdown]
# ## Annotate Amboseli Layers with Geometry Type

# %%
# parameters

annotate_ambo_layers_gdf_dict_params = dict()

# %%
# call the task


annotate_ambo_layers_gdf_dict = (
    annotate_gdf_dict_with_geom_type.set_task_instance_id(
        "annotate_ambo_layers_gdf_dict"
    )
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(gdf_dict=split_ambo_layers_use, **annotate_ambo_layers_gdf_dict_params)
    .call()
)


# %% [markdown]
# ## Create styled layers from split amboseli layers

# %%
# parameters

create_ambo_layers_styled_params = dict()

# %%
# call the task


create_ambo_layers_styled = (
    create_deckgl_layers_from_gdf_dict.set_task_instance_id("create_ambo_layers_styled")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        gdf_dict=annotate_ambo_layers_gdf_dict,
        styles={
            "Rangeland": {
                "extruded": False,
                "get_fill_color": [163, 156, 145],
                "get_line_color": [163, 156, 145],
                "get_line_width": 0.85,
                "stroked": True,
                "filled": True,
                "opacity": 0.75,
            },
            "Rangeland and settlement": {
                "extruded": False,
                "get_fill_color": [255, 160, 122],
                "get_line_color": [255, 160, 122],
                "get_line_width": 0.85,
                "stroked": True,
                "filled": True,
                "opacity": 0.75,
            },
            "Settlement and agriculture": {
                "extruded": False,
                "get_fill_color": [47, 79, 79],
                "get_line_color": [47, 79, 79],
                "get_line_width": 0.85,
                "stroked": True,
                "filled": True,
                "opacity": 0.75,
            },
            "Conservancies": {
                "extruded": False,
                "get_fill_color": [143, 188, 139],
                "get_line_color": [143, 188, 139],
                "get_line_width": 0.85,
                "stroked": True,
                "filled": True,
                "opacity": 0.75,
            },
            "National parks and reserves": {
                "extruded": False,
                "get_fill_color": [77, 102, 0],
                "get_line_color": [77, 102, 0],
                "get_line_width": 0.85,
                "stroked": True,
                "filled": True,
                "opacity": 0.75,
            },
        },
        legends={
            "title": "Map Layers",
            "values": [
                {"label": "Conservancies", "color": "#8fbc8b"},
                {"label": "National park and reserves", "color": "#4d6600"},
                {"label": "Settlement and agriculture", "color": "#2f4f4f"},
                {"label": "Rangeland", "color": "#a39c91"},
                {"label": "Rangeland and settlement", "color": "#ffa07a"},
            ],
        },
        **create_ambo_layers_styled_params,
    )
    .call()
)


# %% [markdown]
# ## Split Amboseli Electric Fence and Boundaries by Land Use

# %%
# parameters

split_ambo_boundaries_params = dict()

# %%
# call the task


split_ambo_boundaries = (
    split_gdf_by_column.set_task_instance_id("split_ambo_boundaries")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        gdf=reproject_ambo_boundaries, column="land_use", **split_ambo_boundaries_params
    )
    .call()
)


# %% [markdown]
# ## Annotate Amboseli Boundaries and Electric Fence with Geometry Type

# %%
# parameters

annotate_ambo_boundaries_params = dict()

# %%
# call the task


annotate_ambo_boundaries = (
    annotate_gdf_dict_with_geom_type.set_task_instance_id("annotate_ambo_boundaries")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(gdf_dict=split_ambo_boundaries, **annotate_ambo_boundaries_params)
    .call()
)


# %% [markdown]
# ## Create styled layers from split amboseli boundaries and electric fence

# %%
# parameters

create_ambo_boundaries_styled_params = dict()

# %%
# call the task


create_ambo_boundaries_styled = (
    create_deckgl_layers_from_gdf_dict.set_task_instance_id(
        "create_ambo_boundaries_styled"
    )
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        gdf_dict=annotate_ambo_boundaries,
        styles={
            "Ranch boundaries": {
                "extruded": False,
                "get_color": [0, 0, 0],
                "get_line_width": 0.95,
                "get_width": 0.95,
                "stroked": True,
                "filled": False,
                "opacity": 0.55,
            },
            "Electric Fence": {
                "extruded": False,
                "get_color": [45, 98, 177],
                "get_width": 5.5,
                "stroked": True,
                "filled": False,
                "opacity": 1.0,
            },
        },
        legends={
            "title": "",
            "values": [
                {"label": "Electric fence", "color": "#2d62b1"},
                {"label": "Ranch boundaries", "color": "#000000"},
            ],
        },
        **create_ambo_boundaries_styled_params,
    )
    .call()
)


# %% [markdown]
# ## Retrieve current events

# %%
# parameters

get_current_events_params = dict()

# %%
# call the task


get_current_events = (
    get_events.set_task_instance_id("get_current_events")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        client=er_client_name,
        time_range=time_range,
        event_columns=[
            "id",
            "time",
            "event_type",
            "event_category",
            "reported_by",
            "serial_number",
            "geometry",
            "created_at",
            "event_details",
        ],
        event_types=["hwc_lvstprd"],
        raise_on_empty=True,
        include_details=True,
        include_updates=False,
        include_related_events=False,
        include_null_geometry=False,
        include_display_values=True,
        **get_current_events_params,
    )
    .call()
)


# %% [markdown]
# ## Get previous period time range

# %%
# parameters

previous_period_range_params = dict()

# %%
# call the task


previous_period_range = (
    shift_previous_period.set_task_instance_id("previous_period_range")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(time_range=time_range, periods_back=5, **previous_period_range_params)
    .call()
)


# %% [markdown]
# ## Retrieve previous period events

# %%
# parameters

get_previous_data_params = dict()

# %%
# call the task


get_previous_data = (
    get_events.set_task_instance_id("get_previous_data")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        client=er_client_name,
        time_range=previous_period_range,
        event_columns=[
            "id",
            "time",
            "event_type",
            "event_category",
            "reported_by",
            "serial_number",
            "geometry",
            "created_at",
            "event_details",
        ],
        event_types=["hwc_lvstprd"],
        raise_on_empty=True,
        include_details=True,
        include_updates=False,
        include_related_events=False,
        include_null_geometry=False,
        include_display_values=True,
        **get_previous_data_params,
    )
    .call()
)


# %% [markdown]
# ## Process current events details

# %%
# parameters

process_current_details_params = dict()

# %%
# call the task


process_current_details = (
    process_events_details.set_task_instance_id("process_current_details")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=get_current_events,
        client=er_client_name,
        map_to_titles=True,
        ordered=True,
        **process_current_details_params,
    )
    .call()
)


# %% [markdown]
# ## Normalize Current Events Column Names

# %%
# parameters

normalize_current_events_params = dict()

# %%
# call the task


normalize_current_events = (
    normalize_json_column.set_task_instance_id("normalize_current_events")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        column="event_details",
        df=process_current_details,
        skip_if_not_exists=True,
        sort_columns=True,
        **normalize_current_events_params,
    )
    .call()
)


# %% [markdown]
# ## Drop column prefix from current events

# %%
# parameters

drop_current_prefix_params = dict()

# %%
# call the task


drop_current_prefix = (
    drop_column_prefix.set_task_instance_id("drop_current_prefix")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=normalize_current_events,
        prefix="event_details__",
        duplicate_strategy="keep_original",
        **drop_current_prefix_params,
    )
    .call()
)


# %% [markdown]
# ## Rename and Filter Current Event Columns

# %%
# parameters

map_current_columns_params = dict()

# %%
# call the task


map_current_columns = (
    map_columns.set_task_instance_id("map_current_columns")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=drop_current_prefix,
        drop_columns=[
            "event_category",
            "event_type",
            "reported_by",
            "event_type_display",
            "Boma coordinate X",
            "Boma coordinate Y",
            "GPS Accuracy",
            "Owner ID number",
            "Owner name",
            "Partners present",
            "Time of verification",
            "Zone",
            "Teams involved",
            "Name of verification officer",
            "Area",
        ],
        retain_columns=[],
        rename_columns={},
        raise_if_not_found=True,
        **map_current_columns_params,
    )
    .call()
)


# %% [markdown]
# ## Filter Events to Target Ranches

# %%
# parameters

retain_current_ranches_params = dict()

# %%
# call the task


retain_current_ranches = (
    filter_row_values.set_task_instance_id("retain_current_ranches")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=map_current_columns,
        column="Ranch",
        values=["Eselengei", "Mbirikani", "Kimana"],
        **retain_current_ranches_params,
    )
    .call()
)


# %% [markdown]
# ## Filter Current Events to Valid Claims Only

# %%
# parameters

filter_current_valid_claims_params = dict()

# %%
# call the task


filter_current_valid_claims = (
    filter_row_values.set_task_instance_id("filter_current_valid_claims")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=retain_current_ranches,
        column="Validity of claim",
        values=["Valid"],
        **filter_current_valid_claims_params,
    )
    .call()
)


# %% [markdown]
# ## Replace Missing Values in Current Events with N/A

# %%
# parameters

replace_current_missing_na_params = dict()

# %%
# call the task


replace_current_missing_na = (
    replace_missing_with_label.set_task_instance_id("replace_current_missing_na")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=filter_current_valid_claims,
        columns=[
            "Did anyone see the predator",
            "Did the predator enter boma",
            "Did the predator scare livestock from inside boma",
            "Does boma have LED simba lights",
            "Does boma have predator proof fencing",
            "Predator Scout present",
            "Tracks present",
        ],
        label="N/A",
        **replace_current_missing_na_params,
    )
    .call()
)


# %% [markdown]
# ## Replace Unknown Values in Current Events

# %%
# parameters

replace_current_missing_unknown_params = dict()

# %%
# call the task


replace_current_missing_unknown = (
    replace_missing_with_label.set_task_instance_id("replace_current_missing_unknown")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=replace_current_missing_na,
        columns=[
            "Boma type",
            "Livestock species",
            "Predator Scout present",
            "Ranch",
            "Time of attack",
            "Type of claim",
            "Validity of claim",
            "Where were the livestock when the attack happened",
            "Animal responsible",
        ],
        label="Unknown",
        **replace_current_missing_unknown_params,
    )
    .call()
)


# %% [markdown]
# ## Convert Current Event Numeric Columns to Integer

# %%
# parameters

convert_current_to_int_params = dict()

# %%
# call the task


convert_current_to_int = (
    convert_to_int.set_task_instance_id("convert_current_to_int")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=replace_current_missing_unknown,
        columns=[
            "Adults injured",
            "Adults killed",
            "Compensation value to owner",
            "Young (<1yr) injured",
            "Young (<1yr) killed",
        ],
        errors="coerce",
        fill_value=0,
        inplace=True,
        **convert_current_to_int_params,
    )
    .call()
)


# %% [markdown]
# ## Process previous events details

# %%
# parameters

process_previous_details_params = dict()

# %%
# call the task


process_previous_details = (
    process_events_details.set_task_instance_id("process_previous_details")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=get_previous_data,
        client=er_client_name,
        map_to_titles=True,
        ordered=True,
        **process_previous_details_params,
    )
    .call()
)


# %% [markdown]
# ## Normalize previous events column

# %%
# parameters

normalize_previous_events_params = dict()

# %%
# call the task


normalize_previous_events = (
    normalize_json_column.set_task_instance_id("normalize_previous_events")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        column="event_details",
        df=process_previous_details,
        skip_if_not_exists=True,
        sort_columns=True,
        **normalize_previous_events_params,
    )
    .call()
)


# %% [markdown]
# ## Drop column prefix from previous events

# %%
# parameters

drop_previous_prefix_params = dict()

# %%
# call the task


drop_previous_prefix = (
    drop_column_prefix.set_task_instance_id("drop_previous_prefix")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=normalize_previous_events,
        prefix="event_details__",
        duplicate_strategy="keep_original",
        **drop_previous_prefix_params,
    )
    .call()
)


# %% [markdown]
# ## Rename and Filter Previous Event Columns

# %%
# parameters

map_previous_columns_params = dict()

# %%
# call the task


map_previous_columns = (
    map_columns.set_task_instance_id("map_previous_columns")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=drop_previous_prefix,
        drop_columns=[
            "event_category",
            "event_type",
            "reported_by",
            "event_type_display",
            "Boma coordinate X",
            "Boma coordinate Y",
            "GPS Accuracy",
            "Owner ID number",
            "Owner name",
            "Partners present",
            "Time of verification",
            "Zone",
            "Teams involved",
            "Name of verification officer",
            "Area",
        ],
        retain_columns=[],
        rename_columns={},
        raise_if_not_found=True,
        **map_previous_columns_params,
    )
    .call()
)


# %% [markdown]
# ## Filter Previous Events to Target Ranches

# %%
# parameters

retain_previous_ranches_params = dict()

# %%
# call the task


retain_previous_ranches = (
    filter_row_values.set_task_instance_id("retain_previous_ranches")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=map_previous_columns,
        column="Ranch",
        values=["Eselengei", "Mbirikani", "Kimana"],
        **retain_previous_ranches_params,
    )
    .call()
)


# %% [markdown]
# ## Filter Previous Events to Valid Claims Only

# %%
# parameters

filter_previous_valid_claims_params = dict()

# %%
# call the task


filter_previous_valid_claims = (
    filter_row_values.set_task_instance_id("filter_previous_valid_claims")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=retain_previous_ranches,
        column="Validity of claim",
        values=["Valid"],
        **filter_previous_valid_claims_params,
    )
    .call()
)


# %% [markdown]
# ## Replace Missing Values in Previous Events with N/A

# %%
# parameters

replace_previous_missing_na_params = dict()

# %%
# call the task


replace_previous_missing_na = (
    replace_missing_with_label.set_task_instance_id("replace_previous_missing_na")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=filter_previous_valid_claims,
        columns=[
            "Did anyone see the predator",
            "Did the predator enter boma",
            "Did the predator scare livestock from inside boma",
            "Does boma have LED simba lights",
            "Does boma have predator proof fencing",
            "Predator Scout present",
            "Tracks present",
        ],
        label="N/A",
        **replace_previous_missing_na_params,
    )
    .call()
)


# %% [markdown]
# ## Replace Unknown Values in Previous Events

# %%
# parameters

replace_previous_missing_unknown_params = dict()

# %%
# call the task


replace_previous_missing_unknown = (
    replace_missing_with_label.set_task_instance_id("replace_previous_missing_unknown")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=replace_previous_missing_na,
        columns=[
            "Boma type",
            "Livestock species",
            "Predator Scout present",
            "Ranch",
            "Time of attack",
            "Type of claim",
            "Validity of claim",
            "Where were the livestock when the attack happened",
            "Animal responsible",
        ],
        label="Unknown",
        **replace_previous_missing_unknown_params,
    )
    .call()
)


# %% [markdown]
# ## Convert Previous Event Numeric Columns to Integer

# %%
# parameters

convert_previous_to_int_params = dict()

# %%
# call the task


convert_previous_to_int = (
    convert_to_int.set_task_instance_id("convert_previous_to_int")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=replace_previous_missing_unknown,
        columns=[
            "Adults injured",
            "Adults killed",
            "Compensation value to owner",
            "Young (<1yr) injured",
            "Young (<1yr) killed",
        ],
        errors="coerce",
        fill_value=0,
        inplace=True,
        **convert_previous_to_int_params,
    )
    .call()
)


# %% [markdown]
# ## Combine current columns to create total livestock killed

# %%
# parameters

current_total_killed_params = dict()

# %%
# call the task


current_total_killed = (
    combine_columns.set_task_instance_id("current_total_killed")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=convert_current_to_int,
        output_column="Total animals killed",
        input_columns=["Adults killed", "Young (<1yr) killed"],
        agg_func="sum",
        **current_total_killed_params,
    )
    .call()
)


# %% [markdown]
# ## Combine previous columns to create total livestock killed

# %%
# parameters

previous_total_killed_params = dict()

# %%
# call the task


previous_total_killed = (
    combine_columns.set_task_instance_id("previous_total_killed")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=convert_previous_to_int,
        output_column="Total animals killed",
        input_columns=["Adults killed", "Young (<1yr) killed"],
        agg_func="sum",
        **previous_total_killed_params,
    )
    .call()
)


# %% [markdown]
# ## Overall Predation Incidents Summary

# %%
# parameters

overall_predation_incidents_params = dict()

# %%
# call the task


overall_predation_incidents = (
    crosstab_summary.set_task_instance_id("overall_predation_incidents")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        data=current_total_killed,
        index_col="Animal responsible",
        column_col="Livestock species",
        aggfunc="sum",
        reset_index=False,
        margins=True,
        value_col=None,
        margins_name="Total",
        **overall_predation_incidents_params,
    )
    .call()
)


# %% [markdown]
# ## Overall Livestock Killed Summary

# %%
# parameters

overall_predation_killed_params = dict()

# %%
# call the task


overall_predation_killed = (
    crosstab_summary.set_task_instance_id("overall_predation_killed")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        data=current_total_killed,
        index_col="Animal responsible",
        column_col="Livestock species",
        value_col="Total animals killed",
        aggfunc="sum",
        reset_index=False,
        margins=True,
        margins_name="Total",
        **overall_predation_killed_params,
    )
    .call()
)


# %% [markdown]
# ## Overall Compensation Value Summary

# %%
# parameters

overall_compensation_summary_params = dict()

# %%
# call the task


overall_compensation_summary = (
    crosstab_summary.set_task_instance_id("overall_compensation_summary")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        data=convert_current_to_int,
        index_col="Animal responsible",
        column_col="Livestock species",
        value_col="Compensation value to owner",
        aggfunc="sum",
        reset_index=False,
        margins=True,
        margins_name="Total",
        **overall_compensation_summary_params,
    )
    .call()
)


# %% [markdown]
# ## Compile Overall Predation Summary Table

# %%
# parameters

overall_summary_table_params = dict()

# %%
# call the task


overall_summary_table = (
    summarize_predation_table.set_task_instance_id("overall_summary_table")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        incidents=overall_predation_incidents,
        kills=overall_predation_killed,
        compensation=overall_compensation_summary,
        **overall_summary_table_params,
    )
    .call()
)


# %% [markdown]
# ## Convert Overall Summary to Integer

# %%
# parameters

convert_overall_summary_int_params = dict()

# %%
# call the task


convert_overall_summary_int = (
    convert_numeric_to_int.set_task_instance_id("convert_overall_summary_int")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(df=overall_summary_table, **convert_overall_summary_int_params)
    .call()
)


# %% [markdown]
# ## Format Overall Summary Numbers

# %%
# parameters

format_overall_summary_params = dict()

# %%
# call the task


format_overall_summary = (
    format_numeric_with_commas.set_task_instance_id("format_overall_summary")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(df=convert_overall_summary_int, **format_overall_summary_params)
    .call()
)


# %% [markdown]
# ## Save Overall Predation Summary Table

# %%
# parameters

persist_overall_table_params = dict()

# %%
# call the task


persist_overall_table = (
    persist_df.set_task_instance_id("persist_overall_table")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=format_overall_summary,
        filename="overall_predation_summary",
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filetype="geoparquet",
        **persist_overall_table_params,
    )
    .call()
)


# %% [markdown]
# ## Split Predation Events by Ranch

# %%
# parameters

split_by_ranch_params = dict()

# %%
# call the task


split_by_ranch = (
    split_groups.set_task_instance_id("split_by_ranch")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(df=current_total_killed, groupers=groupers, **split_by_ranch_params)
    .call()
)


# %% [markdown]
# ## Split Previous Predation Events by Ranch

# %%
# parameters

split_previous_by_ranch_params = dict()

# %%
# call the task


split_previous_by_ranch = (
    split_groups.set_task_instance_id("split_previous_by_ranch")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=previous_total_killed, groupers=groupers, **split_previous_by_ranch_params
    )
    .call()
)


# %% [markdown]
# ## Predation Incidents Summary by Ranch

# %%
# parameters

predation_ranch_incidents_params = dict()

# %%
# call the task


predation_ranch_incidents = (
    crosstab_summary.set_task_instance_id("predation_ranch_incidents")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        index_col="Animal responsible",
        column_col="Livestock species",
        aggfunc="sum",
        reset_index=False,
        margins=True,
        value_col=None,
        margins_name="Total",
        **predation_ranch_incidents_params,
    )
    .mapvalues(argnames=["data"], argvalues=split_by_ranch)
)


# %% [markdown]
# ## Livestock Killed Summary by Ranch

# %%
# parameters

predation_ranch_killed_params = dict()

# %%
# call the task


predation_ranch_killed = (
    crosstab_summary.set_task_instance_id("predation_ranch_killed")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        index_col="Animal responsible",
        column_col="Livestock species",
        value_col="Total animals killed",
        aggfunc="sum",
        reset_index=False,
        margins=True,
        margins_name="Total",
        **predation_ranch_killed_params,
    )
    .mapvalues(argnames=["data"], argvalues=split_by_ranch)
)


# %% [markdown]
# ## Compensation Value Summary by Ranch

# %%
# parameters

predation_ranch_compensation_params = dict()

# %%
# call the task


predation_ranch_compensation = (
    crosstab_summary.set_task_instance_id("predation_ranch_compensation")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        index_col="Animal responsible",
        column_col="Livestock species",
        value_col="Compensation value to owner",
        aggfunc="sum",
        reset_index=False,
        margins=True,
        margins_name="Total",
        **predation_ranch_compensation_params,
    )
    .mapvalues(argnames=["data"], argvalues=split_by_ranch)
)


# %% [markdown]
# ## Combine Ranch Summary Tables

# %%
# parameters

zip_ranch_summaries_params = dict()

# %%
# call the task


zip_ranch_summaries = (
    zip_groupbykey.set_task_instance_id("zip_ranch_summaries")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[
            predation_ranch_incidents,
            predation_ranch_killed,
            predation_ranch_compensation,
        ],
        **zip_ranch_summaries_params,
    )
    .call()
)


# %% [markdown]
# ## Compile Predation Summary Table by Ranch

# %%
# parameters

predation_by_ranch_table_params = dict()

# %%
# call the task


predation_by_ranch_table = (
    summarize_predation_table.set_task_instance_id("predation_by_ranch_table")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(**predation_by_ranch_table_params)
    .mapvalues(
        argnames=["incidents", "kills", "compensation"], argvalues=zip_ranch_summaries
    )
)


# %% [markdown]
# ## Convert Ranch Summary to Integer

# %%
# parameters

convert_ranch_summary_int_params = dict()

# %%
# call the task


convert_ranch_summary_int = (
    convert_numeric_to_int.set_task_instance_id("convert_ranch_summary_int")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(**convert_ranch_summary_int_params)
    .mapvalues(argnames=["df"], argvalues=predation_by_ranch_table)
)


# %% [markdown]
# ## Format Ranch Summary Numbers

# %%
# parameters

format_ranch_summary_params = dict()

# %%
# call the task


format_ranch_summary = (
    format_numeric_with_commas.set_task_instance_id("format_ranch_summary")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(**format_ranch_summary_params)
    .mapvalues(argnames=["df"], argvalues=convert_ranch_summary_int)
)


# %% [markdown]
# ## Extract Ranch Name from Data

# %%
# parameters

get_ranch_name_params = dict()

# %%
# call the task


get_ranch_name = (
    dataframe_column_first_unique_str.set_task_instance_id("get_ranch_name")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(column_name="Ranch", **get_ranch_name_params)
    .mapvalues(argnames=["df"], argvalues=split_by_ranch)
)


# %% [markdown]
# ## Add Ranch Name to Summary Table

# %%
# parameters

combine_ranch_name_table_params = dict()

# %%
# call the task


combine_ranch_name_table = (
    zip_groupbykey.set_task_instance_id("combine_ranch_name_table")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[get_ranch_name, format_ranch_summary],
        **combine_ranch_name_table_params,
    )
    .call()
)


# %% [markdown]
# ## Save Ranch Predation Summary Table

# %%
# parameters

persist_ranch_table_params = dict()

# %%
# call the task


persist_ranch_table = (
    persist_df.set_task_instance_id("persist_ranch_table")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filetype="geoparquet",
        **persist_ranch_table_params,
    )
    .mapvalues(argnames=["filename", "df"], argvalues=combine_ranch_name_table)
)


# %% [markdown]
# ## Summarize Livestock Killed by Claim Type

# %%
# parameters

claim_type_killed_summary_params = dict()

# %%
# call the task


claim_type_killed_summary = (
    crosstab_summary.set_task_instance_id("claim_type_killed_summary")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        data=current_total_killed,
        index_col="Type of claim",
        column_col="Ranch",
        value_col="Total animals killed",
        aggfunc="sum",
        margins=True,
        reset_index=True,
        margins_name="Total",
        **claim_type_killed_summary_params,
    )
    .call()
)


# %% [markdown]
# ## Convert Claim Type Summary to Integer

# %%
# parameters

convert_claim_type_killed_int_params = dict()

# %%
# call the task


convert_claim_type_killed_int = (
    convert_numeric_to_int.set_task_instance_id("convert_claim_type_killed_int")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(df=claim_type_killed_summary, **convert_claim_type_killed_int_params)
    .call()
)


# %% [markdown]
# ## Format Claim Type Summary Numbers

# %%
# parameters

format_claim_type_killed_params = dict()

# %%
# call the task


format_claim_type_killed = (
    format_numeric_with_commas.set_task_instance_id("format_claim_type_killed")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(df=convert_claim_type_killed_int, **format_claim_type_killed_params)
    .call()
)


# %% [markdown]
# ## Save Claim Type Summary Table

# %%
# parameters

persist_claim_type_killed_params = dict()

# %%
# call the task


persist_claim_type_killed = (
    persist_df.set_task_instance_id("persist_claim_type_killed")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=format_claim_type_killed,
        filename="livestock_killed_by_claim_type",
        filetype="geoparquet",
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        **persist_claim_type_killed_params,
    )
    .call()
)


# %% [markdown]
# ## Summarize Livestock Killed by Predator Species

# %%
# parameters

predator_killed_summary_params = dict()

# %%
# call the task


predator_killed_summary = (
    crosstab_summary.set_task_instance_id("predator_killed_summary")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        data=current_total_killed,
        index_col="Animal responsible",
        column_col="Ranch",
        value_col="Total animals killed",
        aggfunc="sum",
        reset_index=True,
        margins=True,
        margins_name="Total",
        **predator_killed_summary_params,
    )
    .call()
)


# %% [markdown]
# ## Convert Predator Summary to Integer

# %%
# parameters

convert_predator_killed_int_params = dict()

# %%
# call the task


convert_predator_killed_int = (
    convert_numeric_to_int.set_task_instance_id("convert_predator_killed_int")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(df=predator_killed_summary, **convert_predator_killed_int_params)
    .call()
)


# %% [markdown]
# ## Format Predator Summary Numbers

# %%
# parameters

format_predator_killed_params = dict()

# %%
# call the task


format_predator_killed = (
    format_numeric_with_commas.set_task_instance_id("format_predator_killed")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(df=convert_predator_killed_int, **format_predator_killed_params)
    .call()
)


# %% [markdown]
# ## Save Predator Summary Table

# %%
# parameters

persist_predator_killed_params = dict()

# %%
# call the task


persist_predator_killed = (
    persist_df.set_task_instance_id("persist_predator_killed")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=format_predator_killed,
        filename="livestock_killed_by_predator_species",
        filetype="geoparquet",
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        **persist_predator_killed_params,
    )
    .call()
)


# %% [markdown]
# ## Summarize Livestock Attacks by Location

# %%
# parameters

location_attack_table_params = dict()

# %%
# call the task


location_attack_table = (
    aggregate_by.set_task_instance_id("location_attack_table")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=current_total_killed,
        group_col="Where were the livestock when the attack happened",
        agg_func="count",
        agg_col=None,
        add_percent=True,
        agg_col_name="Count",
        sort="asc",
        **location_attack_table_params,
    )
    .call()
)


# %% [markdown]
# ## Convert Location Summary to Integer

# %%
# parameters

convert_location_attack_int_params = dict()

# %%
# call the task


convert_location_attack_int = (
    convert_numeric_to_int.set_task_instance_id("convert_location_attack_int")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(df=location_attack_table, **convert_location_attack_int_params)
    .call()
)


# %% [markdown]
# ## Format Location Summary Numbers

# %%
# parameters

format_location_attack_params = dict()

# %%
# call the task


format_location_attack = (
    format_numeric_with_commas.set_task_instance_id("format_location_attack")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(df=convert_location_attack_int, **format_location_attack_params)
    .call()
)


# %% [markdown]
# ## Save Location of Attack Summary Table

# %%
# parameters

persist_location_attack_params = dict()

# %%
# call the task


persist_location_attack = (
    persist_df.set_task_instance_id("persist_location_attack")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=format_location_attack,
        filename="livestock_attacks_by_location",
        filetype="geoparquet",
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        **persist_location_attack_params,
    )
    .call()
)


# %% [markdown]
# ## Summarize Predation Incidents by Predator and Location

# %%
# parameters

generate_predation_summary_table_params = dict()

# %%
# call the task


generate_predation_summary_table = (
    crosstab_summary.set_task_instance_id("generate_predation_summary_table")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        data=current_total_killed,
        index_col="Animal responsible",
        column_col="Where were the livestock when the attack happened",
        value_col="id",
        aggfunc="count",
        reset_index=True,
        margins_name="Total",
        margins=False,
        **generate_predation_summary_table_params,
    )
    .call()
)


# %% [markdown]
# ## Convert Predation Summary to Integer

# %%
# parameters

convert_predation_summary_int_params = dict()

# %%
# call the task


convert_predation_summary_int = (
    convert_numeric_to_int.set_task_instance_id("convert_predation_summary_int")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=generate_predation_summary_table, **convert_predation_summary_int_params
    )
    .call()
)


# %% [markdown]
# ## Count Predation Incidents by Predator

# %%
# parameters

predation_location_incidents_params = dict()

# %%
# call the task


predation_location_incidents = (
    aggregate_by.set_task_instance_id("predation_location_incidents")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=current_total_killed,
        group_col="Animal responsible",
        agg_func="count",
        agg_col_name="Total Incidents",
        sort="asc",
        agg_col=None,
        add_percent=False,
        **predation_location_incidents_params,
    )
    .call()
)


# %% [markdown]
# ## Convert Predation Location Incidents to Integer

# %%
# parameters

convert_pred_incidents_int_params = dict()

# %%
# call the task


convert_pred_incidents_int = (
    convert_numeric_to_int.set_task_instance_id("convert_pred_incidents_int")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(df=predation_location_incidents, **convert_pred_incidents_int_params)
    .call()
)


# %% [markdown]
# ## Calculate Predation Incident Percentages

# %%
# parameters

predation_summary_pct_params = dict()

# %%
# call the task


predation_summary_pct = (
    to_percent.set_task_instance_id("predation_summary_pct")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=convert_predation_summary_int, decimals=1, **predation_summary_pct_params
    )
    .call()
)


# %% [markdown]
# ## Merge Predation Location Table with Percentages

# %%
# parameters

merge_predation_pct_params = dict()

# %%
# call the task


merge_predation_pct = (
    merge_two_dataframes.set_task_instance_id("merge_predation_pct")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        left=predation_summary_pct,
        right=convert_pred_incidents_int,
        on="Animal responsible",
        how="left",
        left_on=None,
        right_on=None,
        left_index=False,
        right_index=False,
        fillna_value=None,
        **merge_predation_pct_params,
    )
    .call()
)


# %% [markdown]
# ## Reorder Predation Location Summary Columns

# %%
# parameters

reorder_predation_summary_params = dict()

# %%
# call the task


reorder_predation_summary = (
    reorder_cols.set_task_instance_id("reorder_predation_summary")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=merge_predation_pct,
        cols=[
            "Animal responsible",
            "Total Incidents",
            "Inside Boma",
            "Within 200m of Boma",
            "More than 200m from Boma",
        ],
        **reorder_predation_summary_params,
    )
    .call()
)


# %% [markdown]
# ## Save Predation by Location and Predator Table

# %%
# parameters

persist_predation_sum_location_params = dict()

# %%
# call the task


persist_predation_sum_location = (
    persist_df.set_task_instance_id("persist_predation_sum_location")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=reorder_predation_summary,
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename="predation_incidents_by_predator_and_location",
        filetype="geoparquet",
        **persist_predation_sum_location_params,
    )
    .call()
)


# %% [markdown]
# ## Summarize Predation Incidents by Boma Type

# %%
# parameters

boma_pred_incidents_params = dict()

# %%
# call the task


boma_pred_incidents = (
    aggregate_by.set_task_instance_id("boma_pred_incidents")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=current_total_killed,
        group_col="Boma type",
        agg_func="count",
        agg_col_name="Total Incidents",
        agg_col=None,
        add_percent=True,
        sort="asc",
        **boma_pred_incidents_params,
    )
    .call()
)


# %% [markdown]
# ## Format Boma Summary Numbers

# %%
# parameters

format_boma_incidents_params = dict()

# %%
# call the task


format_boma_incidents = (
    format_numeric_with_commas.set_task_instance_id("format_boma_incidents")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(df=boma_pred_incidents, **format_boma_incidents_params)
    .call()
)


# %% [markdown]
# ## Save Boma Predation Incidents Table

# %%
# parameters

persist_boma_incidents_params = dict()

# %%
# call the task


persist_boma_incidents = (
    persist_df.set_task_instance_id("persist_boma_incidents")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=format_boma_incidents,
        filename="predation_incidents_by_boma_type",
        filetype="geoparquet",
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        **persist_boma_incidents_params,
    )
    .call()
)


# %% [markdown]
# ## Assign animal responsible colors

# %%
# parameters

animal_responsible_colors_params = dict()

# %%
# call the task


animal_responsible_colors = (
    get_color_map.set_task_instance_id("animal_responsible_colors")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=current_total_killed,
        col="Animal responsible",
        color_dict={
            "Caracal": "#fd7f6f",
            "Cheetah": "#7eb0d5",
            "Elephant": "#b2e061",
            "Hyena": "#bd7ebe",
            "Jackal": "#ffb55a",
            "Leopard": "#ffee65",
            "Lion": "#beb9db",
            "Unknown": "#cfcfc4",
            "Wild dog": "#4cc9b0",
        },
        **animal_responsible_colors_params,
    )
    .call()
)


# %% [markdown]
# ## Assign boma type colors

# %%
# parameters

boma_type_colors_params = dict()

# %%
# call the task


boma_type_colors = (
    get_color_map.set_task_instance_id("boma_type_colors")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=current_total_killed,
        col="Boma type",
        color_dict={"Permanent": "#fd7f6f", "Temporary": "#7eb0d5"},
        **boma_type_colors_params,
    )
    .call()
)


# %% [markdown]
# ## Assign livestock location colors

# %%
# parameters

livestock_location_colors_params = dict()

# %%
# call the task


livestock_location_colors = (
    get_color_map.set_task_instance_id("livestock_location_colors")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=current_total_killed,
        col="Where were the livestock when the attack happened",
        color_dict={
            "Inside Boma": "#fd7f6f",
            "More than 200m from Boma": "#7eb0d5",
            "Within 200m of Boma": "#b2e061",
        },
        **livestock_location_colors_params,
    )
    .call()
)


# %% [markdown]
# ## Assign Color Scheme for Claim Types

# %%
# parameters

claim_colors_params = dict()

# %%
# call the task


claim_colors = (
    get_color_map.set_task_instance_id("claim_colors")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=current_total_killed,
        col="Type of claim",
        color_dict={
            "Bad Boma": "#beb9db",
            "No Penalty": "#b2e061",
            "Lost in the Bush": "#7eb0d5",
        },
        **claim_colors_params,
    )
    .call()
)


# %% [markdown]
# ## Assign Color Scheme for Ranches

# %%
# parameters

ranch_colors_params = dict()

# %%
# call the task


ranch_colors = (
    get_color_map.set_task_instance_id("ranch_colors")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=current_total_killed,
        col="Ranch",
        color_dict={
            "Eselengei": "#fd7f6f",
            "Kimana": "#7eb0d5",
            "Mbirikani": "#b2e061",
        },
        **ranch_colors_params,
    )
    .call()
)


# %% [markdown]
# ## Assign Color Scheme for Livestock Species

# %%
# parameters

livestock_species_colors_params = dict()

# %%
# call the task


livestock_species_colors = (
    get_color_map.set_task_instance_id("livestock_species_colors")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=current_total_killed,
        col="Livestock species",
        color_dict={"Shoat": "#0000ff", "Cow": "#8b0000", "Donkey": "#ffff00"},
        **livestock_species_colors_params,
    )
    .call()
)


# %% [markdown]
# ## Map animal responsible colors to current events

# %%
# parameters

map_animal_colors_params = dict()

# %%
# call the task


map_animal_colors = (
    map_color_column_value.set_task_instance_id("map_animal_colors")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=current_total_killed,
        col="Animal responsible",
        mapping=animal_responsible_colors,
        new_col="animal_responsible_colors",
        **map_animal_colors_params,
    )
    .call()
)


# %% [markdown]
# ## Map boma type colors to current events

# %%
# parameters

map_boma_colors_params = dict()

# %%
# call the task


map_boma_colors = (
    map_color_column_value.set_task_instance_id("map_boma_colors")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=map_animal_colors,
        col="Boma type",
        mapping=boma_type_colors,
        new_col="boma_type_colors",
        **map_boma_colors_params,
    )
    .call()
)


# %% [markdown]
# ## Map livestock location colors to current events

# %%
# parameters

map_location_colors_params = dict()

# %%
# call the task


map_location_colors = (
    map_color_column_value.set_task_instance_id("map_location_colors")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=map_boma_colors,
        col="Where were the livestock when the attack happened",
        mapping=livestock_location_colors,
        new_col="livestock_location_colors",
        **map_location_colors_params,
    )
    .call()
)


# %% [markdown]
# ## Map claim type colors to current events

# %%
# parameters

map_claim_colors_params = dict()

# %%
# call the task


map_claim_colors = (
    map_color_column_value.set_task_instance_id("map_claim_colors")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=map_location_colors,
        col="Type of claim",
        mapping=claim_colors,
        new_col="type_of_claim_colors",
        **map_claim_colors_params,
    )
    .call()
)


# %% [markdown]
# ## Map ranch colors to current events

# %%
# parameters

map_ranch_colors_params = dict()

# %%
# call the task


map_ranch_colors = (
    map_color_column_value.set_task_instance_id("map_ranch_colors")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=map_claim_colors,
        col="Ranch",
        mapping=ranch_colors,
        new_col="ranch_colors",
        **map_ranch_colors_params,
    )
    .call()
)


# %% [markdown]
# ## Map livestock species colors to current events

# %%
# parameters

map_livestock_colors_params = dict()

# %%
# call the task


map_livestock_colors = (
    map_color_column_value.set_task_instance_id("map_livestock_colors")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=map_ranch_colors,
        col="Livestock species",
        mapping=livestock_species_colors,
        new_col="livestock_species_colors",
        **map_livestock_colors_params,
    )
    .call()
)


# %% [markdown]
# ## Draw total livestock killed pie chart

# %%
# parameters

total_livestock_pie_chart_params = dict(
    widget_id=...,
)

# %%
# call the task


total_livestock_pie_chart = (
    draw_pie_chart.set_task_instance_id("total_livestock_pie_chart")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        dataframe=map_livestock_colors,
        value_column="Total animals killed",
        label_column="Animal responsible",
        color_column="animal_responsible_colors",
        plot_style={"textinfo": "percent+label+value"},
        layout_style={"font_size": 15, "font_style": "normal", "showlegend": True},
        **total_livestock_pie_chart_params,
    )
    .call()
)


# %% [markdown]
# ## Persist total livestock killed pie chart

# %%
# parameters

persist_total_livestock_pie_params = dict()

# %%
# call the task


persist_total_livestock_pie = (
    persist_text.set_task_instance_id("persist_total_livestock_pie")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        text=total_livestock_pie_chart,
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename="livestock_killed_by_predator_pie.html",
        filename_suffix=None,
        **persist_total_livestock_pie_params,
    )
    .call()
)


# %% [markdown]
# ## Convert total livestock killed pie chart to png

# %%
# parameters

convert_total_livestock_pie_png_params = dict()

# %%
# call the task


convert_total_livestock_pie_png = (
    html_to_png.set_task_instance_id("convert_total_livestock_pie_png")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        html_path=persist_total_livestock_pie,
        config={
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 50,
            "max_concurrent_pages": 1,
        },
        **convert_total_livestock_pie_png_params,
    )
    .call()
)


# %% [markdown]
# ## Draw compensation value by predator pie chart

# %%
# parameters

compensation_pred_pie_chart_params = dict(
    widget_id=...,
)

# %%
# call the task


compensation_pred_pie_chart = (
    draw_pie_chart.set_task_instance_id("compensation_pred_pie_chart")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        dataframe=map_livestock_colors,
        value_column="Compensation value to owner",
        label_column="Animal responsible",
        color_column="animal_responsible_colors",
        plot_style={"textinfo": "percent+label+value"},
        layout_style={"font_size": 15, "font_style": "normal", "showlegend": True},
        **compensation_pred_pie_chart_params,
    )
    .call()
)


# %% [markdown]
# ## Persist compensation by predator pie chart

# %%
# parameters

persist_compensation_pred_pie_params = dict()

# %%
# call the task


persist_compensation_pred_pie = (
    persist_text.set_task_instance_id("persist_compensation_pred_pie")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        text=compensation_pred_pie_chart,
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename="compensation_value_by_predator_pie.html",
        filename_suffix=None,
        **persist_compensation_pred_pie_params,
    )
    .call()
)


# %% [markdown]
# ## Convert compensation by predator pie chart to png

# %%
# parameters

convert_comp_pred_pie_png_params = dict()

# %%
# call the task


convert_comp_pred_pie_png = (
    html_to_png.set_task_instance_id("convert_comp_pred_pie_png")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        html_path=persist_compensation_pred_pie,
        config={
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 50,
            "max_concurrent_pages": 1,
        },
        **convert_comp_pred_pie_png_params,
    )
    .call()
)


# %% [markdown]
# ## Draw compensation value by ranch pie chart

# %%
# parameters

compensation_ranch_pie_chart_params = dict(
    widget_id=...,
)

# %%
# call the task


compensation_ranch_pie_chart = (
    draw_pie_chart.set_task_instance_id("compensation_ranch_pie_chart")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        dataframe=map_livestock_colors,
        value_column="Compensation value to owner",
        label_column="Ranch",
        color_column="ranch_colors",
        plot_style={"textinfo": "percent+label+value"},
        layout_style={"font_size": 15, "font_style": "normal", "showlegend": True},
        **compensation_ranch_pie_chart_params,
    )
    .call()
)


# %% [markdown]
# ## Persist compensation by ranch pie chart

# %%
# parameters

persist_compensation_ranch_pie_params = dict()

# %%
# call the task


persist_compensation_ranch_pie = (
    persist_text.set_task_instance_id("persist_compensation_ranch_pie")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        text=compensation_ranch_pie_chart,
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename="compensation_value_by_ranch_pie.html",
        filename_suffix=None,
        **persist_compensation_ranch_pie_params,
    )
    .call()
)


# %% [markdown]
# ## Convert compensation by ranch pie chart to png

# %%
# parameters

convert_comp_ranch_pie_png_params = dict()

# %%
# call the task


convert_comp_ranch_pie_png = (
    html_to_png.set_task_instance_id("convert_comp_ranch_pie_png")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        html_path=persist_compensation_ranch_pie,
        config={
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 50,
            "max_concurrent_pages": 1,
        },
        **convert_comp_ranch_pie_png_params,
    )
    .call()
)


# %% [markdown]
# ## Draw total animals killed by claim type stacked bar chart

# %%
# parameters

killed_claim_stacked_bar_params = dict(
    widget_id=...,
)

# %%
# call the task


killed_claim_stacked_bar = (
    draw_custom_stacked_bar_chart.set_task_instance_id("killed_claim_stacked_bar")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        dataframe=map_livestock_colors,
        x_axis="Ranch",
        y_axis="Total animals killed",
        agg_function="sum",
        stack_column="Type of claim",
        color_column="type_of_claim_colors",
        category_order=None,
        stack_order=["Lost in the Bush", "No Penalty", "Bad Boma"],
        plot_style={"xperiodalignment": "middle"},
        layout_style={
            "font_size": 15,
            "font_style": "normal",
            "showlegend": True,
            "bargap": 0.1,
            "font_color": "#222222",
            "plot_bgcolor": "#f5f5f5",
            "title_x": 0.5,
            "xaxis": {"title": "Ranch"},
            "yaxis": {"title": "Number of livestock killed"},
        },
        **killed_claim_stacked_bar_params,
    )
    .call()
)


# %% [markdown]
# ## Persist total animals killed by claim type stacked bar chart

# %%
# parameters

persist_killed_claim_stacked_bar_params = dict()

# %%
# call the task


persist_killed_claim_stacked_bar = (
    persist_text.set_task_instance_id("persist_killed_claim_stacked_bar")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        text=killed_claim_stacked_bar,
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename="livestock_killed_by_claim_type_bar.html",
        filename_suffix=None,
        **persist_killed_claim_stacked_bar_params,
    )
    .call()
)


# %% [markdown]
# ## Convert total animals killed by claim type stacked bar chart to png

# %%
# parameters

convert_total_lvstk_stacked_png_params = dict()

# %%
# call the task


convert_total_lvstk_stacked_png = (
    html_to_png.set_task_instance_id("convert_total_lvstk_stacked_png")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        html_path=persist_killed_claim_stacked_bar,
        config={
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 50,
            "max_concurrent_pages": 1,
        },
        **convert_total_lvstk_stacked_png_params,
    )
    .call()
)


# %% [markdown]
# ## Draw total number of claims by ranch stacked bar chart

# %%
# parameters

claims_ranch_stacked_bar_params = dict(
    widget_id=...,
)

# %%
# call the task


claims_ranch_stacked_bar = (
    draw_custom_stacked_bar_chart.set_task_instance_id("claims_ranch_stacked_bar")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        dataframe=map_livestock_colors,
        x_axis="Ranch",
        y_axis="id",
        agg_function="count",
        stack_column="Type of claim",
        color_column="type_of_claim_colors",
        category_order=None,
        stack_order=["Lost in the Bush", "No Penalty", "Bad Boma"],
        plot_style={"xperiodalignment": "middle"},
        layout_style={
            "font_size": 15,
            "font_style": "normal",
            "showlegend": True,
            "bargap": 0.1,
            "font_color": "#222222",
            "plot_bgcolor": "#f5f5f5",
            "title_x": 0.5,
            "xaxis": {"title": "Ranch"},
            "yaxis": {"title": "Number of claims"},
        },
        **claims_ranch_stacked_bar_params,
    )
    .call()
)


# %% [markdown]
# ## Persist total number of claims by ranch stacked bar chart

# %%
# parameters

persist_claims_ranch_stacked_bar_params = dict()

# %%
# call the task


persist_claims_ranch_stacked_bar = (
    persist_text.set_task_instance_id("persist_claims_ranch_stacked_bar")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        text=claims_ranch_stacked_bar,
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename="claim_count_by_type_bar.html",
        filename_suffix=None,
        **persist_claims_ranch_stacked_bar_params,
    )
    .call()
)


# %% [markdown]
# ## Convert total number of claims by ranch stacked bar chart to png

# %%
# parameters

convert_claims_ranch_stacked_png_params = dict()

# %%
# call the task


convert_claims_ranch_stacked_png = (
    html_to_png.set_task_instance_id("convert_claims_ranch_stacked_png")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        html_path=persist_claims_ranch_stacked_bar,
        config={
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 50,
            "max_concurrent_pages": 1,
        },
        **convert_claims_ranch_stacked_png_params,
    )
    .call()
)


# %% [markdown]
# ## Draw total animals killed by predator species 100% stacked bar chart

# %%
# parameters

killed_predator_stacked_bar_params = dict(
    widget_id=...,
)

# %%
# call the task


killed_predator_stacked_bar = (
    draw_custom_100_stacked_bar_chart.set_task_instance_id(
        "killed_predator_stacked_bar"
    )
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        dataframe=map_livestock_colors,
        x_axis="Ranch",
        agg_function="sum",
        y_axis="Total animals killed",
        stack_column="Animal responsible",
        color_column="animal_responsible_colors",
        stack_order=["Hyena", "Jackal", "Lion"],
        plot_style={"xperiodalignment": "middle"},
        layout_style={
            "font_size": 15,
            "font_style": "normal",
            "showlegend": True,
            "bargap": 0.1,
            "font_color": "#222222",
            "plot_bgcolor": "#f5f5f5",
            "title_x": 0.5,
            "xaxis": {"title": "Ranch"},
            "yaxis": {"title": "Percentage"},
        },
        **killed_predator_stacked_bar_params,
    )
    .call()
)


# %% [markdown]
# ## Persist total animals killed by predator species 100% stacked bar chart

# %%
# parameters

persist_killed_pred_stacked_bar_params = dict()

# %%
# call the task


persist_killed_pred_stacked_bar = (
    persist_text.set_task_instance_id("persist_killed_pred_stacked_bar")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        text=killed_predator_stacked_bar,
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename="livestock_killed_by_predator_pct_bar.html",
        filename_suffix=None,
        **persist_killed_pred_stacked_bar_params,
    )
    .call()
)


# %% [markdown]
# ## Convert total animals killed by predator species 100% stacked bar chart to png

# %%
# parameters

convert_killed_pred_stacked_png_params = dict()

# %%
# call the task


convert_killed_pred_stacked_png = (
    html_to_png.set_task_instance_id("convert_killed_pred_stacked_png")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        html_path=persist_killed_pred_stacked_bar,
        config={
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 50,
            "max_concurrent_pages": 1,
        },
        **convert_killed_pred_stacked_png_params,
    )
    .call()
)


# %% [markdown]
# ## Draw location of livestock attack pie chart

# %%
# parameters

location_attack_pie_chart_params = dict(
    widget_id=...,
)

# %%
# call the task


location_attack_pie_chart = (
    draw_pie_chart.set_task_instance_id("location_attack_pie_chart")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        dataframe=map_location_colors,
        value_column="Where were the livestock when the attack happened",
        label_column="Where were the livestock when the attack happened",
        color_column="livestock_location_colors",
        plot_style={"textinfo": "percent+label+value"},
        layout_style={"font_size": 15, "font_style": "normal", "showlegend": True},
        **location_attack_pie_chart_params,
    )
    .call()
)


# %% [markdown]
# ## Persist location of livestock attack pie chart

# %%
# parameters

persist_location_attack_pie_params = dict()

# %%
# call the task


persist_location_attack_pie = (
    persist_text.set_task_instance_id("persist_location_attack_pie")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        text=location_attack_pie_chart,
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename="livestock_attack_location_pie.html",
        filename_suffix=None,
        **persist_location_attack_pie_params,
    )
    .call()
)


# %% [markdown]
# ## Convert location of livestock attack pie chart to png

# %%
# parameters

convert_location_attack_pie_png_params = dict()

# %%
# call the task


convert_location_attack_pie_png = (
    html_to_png.set_task_instance_id("convert_location_attack_pie_png")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        html_path=persist_location_attack_pie,
        config={
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 50,
            "max_concurrent_pages": 1,
        },
        **convert_location_attack_pie_png_params,
    )
    .call()
)


# %% [markdown]
# ## Assign boma predation type colors

# %%
# parameters

boma_predation_type_colors_params = dict()

# %%
# call the task


boma_predation_type_colors = (
    get_color_map.set_task_instance_id("boma_predation_type_colors")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=boma_pred_incidents,
        col="Boma type",
        color_dict={"Permanent": "#fd7f6f", "Temporary": "#7eb0d5"},
        **boma_predation_type_colors_params,
    )
    .call()
)


# %% [markdown]
# ## Map boma predation type colors

# %%
# parameters

map_boma_pred_colors_params = dict()

# %%
# call the task


map_boma_pred_colors = (
    map_color_column_value.set_task_instance_id("map_boma_pred_colors")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=boma_pred_incidents,
        col="Boma type",
        mapping=boma_predation_type_colors,
        new_col="boma_type_colors",
        **map_boma_pred_colors_params,
    )
    .call()
)


# %% [markdown]
# ## Draw type of bomas targeted in livestock attacks pie chart

# %%
# parameters

boma_type_pie_chart_params = dict(
    widget_id=...,
)

# %%
# call the task


boma_type_pie_chart = (
    draw_pie_chart.set_task_instance_id("boma_type_pie_chart")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        dataframe=map_boma_pred_colors,
        value_column="Total Incidents",
        label_column="Boma type",
        color_column="boma_type_colors",
        plot_style={"textinfo": "percent+label+value"},
        layout_style={"font_size": 15, "font_style": "normal", "showlegend": True},
        **boma_type_pie_chart_params,
    )
    .call()
)


# %% [markdown]
# ## Persist type of bomas targeted in livestock attacks pie chart

# %%
# parameters

persist_boma_type_pie_chart_params = dict()

# %%
# call the task


persist_boma_type_pie_chart = (
    persist_text.set_task_instance_id("persist_boma_type_pie_chart")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        text=boma_type_pie_chart,
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename="boma_type_targeted_pie.html",
        filename_suffix=None,
        **persist_boma_type_pie_chart_params,
    )
    .call()
)


# %% [markdown]
# ## Convert type of bomas targeted in livestock attacks pie chart to png

# %%
# parameters

convert_boma_type_pie_png_params = dict()

# %%
# call the task


convert_boma_type_pie_png = (
    html_to_png.set_task_instance_id("convert_boma_type_pie_png")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        html_path=persist_boma_type_pie_chart,
        config={
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 50,
            "max_concurrent_pages": 1,
        },
        **convert_boma_type_pie_png_params,
    )
    .call()
)


# %% [markdown]
# ## Draw time of attack bar chart

# %%
# parameters

time_of_attack_bar_chart_params = dict(
    widget_id=...,
)

# %%
# call the task


time_of_attack_bar_chart = (
    draw_custom_bar_chart.set_task_instance_id("time_of_attack_bar_chart")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        dataframe=map_livestock_colors,
        category="Time of attack",
        category_order=[
            "Morning (06:00 - 11:59)",
            "Afternoon (12:00 - 16:59)",
            "Evening (17:00 - 19:59)",
            "Night (20:00 - 05:59)",
        ],
        bar_chart_configs=[
            {
                "column": "Time of attack",
                "agg_func": "count",
                "label": "Percent",
                "show_label": True,
            }
        ],
        layout_kwargs={
            "font_size": 15,
            "plot_bgcolor": "#f5f5f5",
            "showlegend": False,
            "xaxis": {"title": "Time of day"},
            "yaxis": {"title": "Number of predation incidents"},
        },
        **time_of_attack_bar_chart_params,
    )
    .call()
)


# %% [markdown]
# ## Persist time of attack bar chart

# %%
# parameters

persist_time_of_attack_bar_chart_params = dict()

# %%
# call the task


persist_time_of_attack_bar_chart = (
    persist_text.set_task_instance_id("persist_time_of_attack_bar_chart")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        text=time_of_attack_bar_chart,
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename="predation_incidents_by_time_of_day_bar.html",
        filename_suffix=None,
        **persist_time_of_attack_bar_chart_params,
    )
    .call()
)


# %% [markdown]
# ## Convert time of attack bar chart to png

# %%
# parameters

convert_time_attack_bar_png_params = dict()

# %%
# call the task


convert_time_attack_bar_png = (
    html_to_png.set_task_instance_id("convert_time_attack_bar_png")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        html_path=persist_time_of_attack_bar_chart,
        config={
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 50,
            "max_concurrent_pages": 1,
        },
        **convert_time_attack_bar_png_params,
    )
    .call()
)


# %% [markdown]
# ## Generate density grid

# %%
# parameters

generate_predation_grids_params = dict()

# %%
# call the task


generate_predation_grids = (
    generate_density_grid.set_task_instance_id("generate_predation_grids")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        features_gdf=map_livestock_colors,
        cell_size_meters=2000,
        geometry_type="point",
        **generate_predation_grids_params,
    )
    .call()
)


# %% [markdown]
# ## Sort density grid values in ascending order

# %%
# parameters

sort_density_grid_params = dict()

# %%
# call the task


sort_density_grid = (
    sort_values.set_task_instance_id("sort_density_grid")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=generate_predation_grids,
        column_name="density",
        ascending=True,
        na_position="last",
        **sort_density_grid_params,
    )
    .call()
)


# %% [markdown]
# ## Apply bin classification to density grid

# %%
# parameters

density_bins_params = dict()

# %%
# call the task


density_bins = (
    apply_classification.set_task_instance_id("density_bins")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        input_column_name="density",
        output_column_name="density_bins",
        df=sort_density_grid,
        label_options={"label_ranges": True, "label_decimals": 0},
        classification_options={"scheme": "equal_interval", "k": 5},
        **density_bins_params,
    )
    .call()
)


# %% [markdown]
# ## Apply colormap to density bins

# %%
# parameters

apply_density_colormap_params = dict()

# %%
# call the task


apply_density_colormap = (
    apply_color_map.set_task_instance_id("apply_density_colormap")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=density_bins,
        input_column_name="density_bins",
        output_column_name="density_bins_colors",
        colormap=["#FFF7BC", "#FD8D3C", "#F03B20", "#BD0026", "#99000D"],
        **apply_density_colormap_params,
    )
    .call()
)


# %% [markdown]
# ## Create density grid layers

# %%
# parameters

generate_density_layers_params = dict()

# %%
# call the task


generate_density_layers = (
    create_geojson_layer.set_task_instance_id("generate_density_layers")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        layer_style={
            "filled": True,
            "stroked": True,
            "extruded": False,
            "wireframe": False,
            "get_fill_color": "density_bins_colors",
            "get_line_color": [0, 0, 0, 200],
            "opacity": 0.55,
            "get_line_width": 0.35,
            "get_elevation": 0,
            "get_point_radius": 1,
            "line_width_units": "pixels",
            "line_width_scale": 1,
            "line_width_min_pixels": 1,
            "line_width_max_pixels": 5,
        },
        legend={
            "title": "Density",
            "label_column": "density_bins",
            "color_column": "density_bins_colors",
            "sort": None,
            "label_suffix": None,
        },
        geodataframe=apply_density_colormap,
        **generate_density_layers_params,
    )
    .call()
)


# %% [markdown]
# ## Overlay Predation Density Grid on Amboseli Map

# %%
# parameters

combine_ambo_grid_layers_params = dict()

# %%
# call the task


combine_ambo_grid_layers = (
    combine_deckgl_map_layers.set_task_instance_id("combine_ambo_grid_layers")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        static_layers=[create_ambo_layers_styled, create_ambo_boundaries_styled],
        grouped_layers=[generate_density_layers],
        **combine_ambo_grid_layers_params,
    )
    .call()
)


# %% [markdown]
# ## Calculate Zoom Level for Predation Density Map

# %%
# parameters

density_zoom_value_params = dict()

# %%
# call the task


density_zoom_value = (
    view_state_deck_gdf.set_task_instance_id("density_zoom_value")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        pitch=0, bearing=0, gdf=apply_density_colormap, **density_zoom_value_params
    )
    .call()
)


# %% [markdown]
# ## Draw Predation Incident Density Map

# %%
# parameters

draw_density_map_params = dict(
    widget_id=...,
)

# %%
# call the task


draw_density_map = (
    draw_map.set_task_instance_id("draw_density_map")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        tile_layers=configure_base_maps,
        static=False,
        title=None,
        max_zoom=10,
        legend_style={"placement": "bottom-right"},
        geo_layers=combine_ambo_grid_layers,
        view_state=density_zoom_value,
        **draw_density_map_params,
    )
    .call()
)


# %% [markdown]
# ## Save Predation Density Map

# %%
# parameters

persist_density_urls_params = dict(
    filename_suffix=...,
)

# %%
# call the task


persist_density_urls = (
    persist_text.set_task_instance_id("persist_density_urls")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        text=draw_density_map,
        filename="predation_incident_density_map.html",
        **persist_density_urls_params,
    )
    .call()
)


# %% [markdown]
# ## Convert predation density map to png

# %%
# parameters

convert_density_map_png_params = dict()

# %%
# call the task


convert_density_map_png = (
    html_to_png.set_task_instance_id("convert_density_map_png")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        html_path=persist_density_urls,
        config={
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 40000,
            "max_concurrent_pages": 1,
        },
        **convert_density_map_png_params,
    )
    .call()
)


# %% [markdown]
# ## Filter Events to Boma Attacks Only

# %%
# parameters

filter_boma_events_params = dict()

# %%
# call the task


filter_boma_events = (
    filter_df.set_task_instance_id("filter_boma_events")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=map_livestock_colors,
        column_name="Where were the livestock when the attack happened",
        op="equal",
        reset_index=False,
        value="Inside Boma",
        **filter_boma_events_params,
    )
    .call()
)


# %% [markdown]
# ## Generate boma density grid

# %%
# parameters

generate_boma_density_grid_params = dict()

# %%
# call the task


generate_boma_density_grid = (
    generate_density_grid.set_task_instance_id("generate_boma_density_grid")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        features_gdf=filter_boma_events,
        cell_size_meters=2000,
        geometry_type="point",
        **generate_boma_density_grid_params,
    )
    .call()
)


# %% [markdown]
# ## Sort boma density grid values in ascending order

# %%
# parameters

sort_boma_density_grid_params = dict()

# %%
# call the task


sort_boma_density_grid = (
    sort_values.set_task_instance_id("sort_boma_density_grid")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=generate_boma_density_grid,
        column_name="density",
        ascending=True,
        na_position="last",
        **sort_boma_density_grid_params,
    )
    .call()
)


# %% [markdown]
# ## Apply bin classification to boma density grid

# %%
# parameters

density_boma_bins_params = dict()

# %%
# call the task


density_boma_bins = (
    apply_classification.set_task_instance_id("density_boma_bins")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        input_column_name="density",
        output_column_name="density_bins",
        df=sort_boma_density_grid,
        label_options={"label_ranges": True, "label_decimals": 0},
        classification_options={"scheme": "equal_interval", "k": 5},
        **density_boma_bins_params,
    )
    .call()
)


# %% [markdown]
# ## Apply colormap to boma density bins

# %%
# parameters

apply_density_boma_colormap_params = dict()

# %%
# call the task


apply_density_boma_colormap = (
    apply_color_map.set_task_instance_id("apply_density_boma_colormap")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=density_boma_bins,
        input_column_name="density_bins",
        output_column_name="density_bins_colors",
        colormap=["#FFF7BC", "#FD8D3C", "#F03B20", "#BD0026", "#99000D"],
        **apply_density_boma_colormap_params,
    )
    .call()
)


# %% [markdown]
# ## Create boma density grid layers

# %%
# parameters

generate_boma_density_layers_params = dict()

# %%
# call the task


generate_boma_density_layers = (
    create_geojson_layer.set_task_instance_id("generate_boma_density_layers")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        layer_style={
            "filled": True,
            "stroked": True,
            "extruded": False,
            "wireframe": False,
            "get_fill_color": "density_bins_colors",
            "get_line_color": [0, 0, 0, 200],
            "opacity": 0.55,
            "get_line_width": 0.35,
            "get_elevation": 0,
            "get_point_radius": 1,
            "line_width_units": "pixels",
            "line_width_scale": 1,
            "line_width_min_pixels": 1,
            "line_width_max_pixels": 5,
        },
        legend={
            "title": "Density",
            "label_column": "density_bins",
            "color_column": "density_bins_colors",
            "sort": None,
            "label_suffix": None,
        },
        geodataframe=apply_density_boma_colormap,
        **generate_boma_density_layers_params,
    )
    .call()
)


# %% [markdown]
# ## Overlay Boma Density Grid on Amboseli Map

# %%
# parameters

combine_ambo_boma_grid_layers_params = dict()

# %%
# call the task


combine_ambo_boma_grid_layers = (
    combine_deckgl_map_layers.set_task_instance_id("combine_ambo_boma_grid_layers")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        static_layers=[create_ambo_layers_styled, create_ambo_boundaries_styled],
        grouped_layers=[generate_boma_density_layers],
        **combine_ambo_boma_grid_layers_params,
    )
    .call()
)


# %% [markdown]
# ## Calculate Zoom Level for Boma Density Map

# %%
# parameters

density_boma_zoom_value_params = dict()

# %%
# call the task


density_boma_zoom_value = (
    view_state_deck_gdf.set_task_instance_id("density_boma_zoom_value")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        pitch=0,
        bearing=0,
        gdf=apply_density_boma_colormap,
        **density_boma_zoom_value_params,
    )
    .call()
)


# %% [markdown]
# ## Draw boma density map

# %%
# parameters

draw_density_boma_map_params = dict(
    widget_id=...,
)

# %%
# call the task


draw_density_boma_map = (
    draw_map.set_task_instance_id("draw_density_boma_map")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        tile_layers=configure_base_maps,
        static=False,
        title=None,
        max_zoom=10,
        legend_style={"placement": "bottom-right"},
        geo_layers=combine_ambo_boma_grid_layers,
        view_state=density_boma_zoom_value,
        **draw_density_boma_map_params,
    )
    .call()
)


# %% [markdown]
# ## Persist boma density map

# %%
# parameters

persist_density_boma_urls_params = dict(
    filename_suffix=...,
)

# %%
# call the task


persist_density_boma_urls = (
    persist_text.set_task_instance_id("persist_density_boma_urls")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        text=draw_density_boma_map,
        filename="boma_predation_density_map.html",
        **persist_density_boma_urls_params,
    )
    .call()
)


# %% [markdown]
# ## Convert boma density map to png

# %%
# parameters

convert_density_boma_map_png_params = dict()

# %%
# call the task


convert_density_boma_map_png = (
    html_to_png.set_task_instance_id("convert_density_boma_map_png")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        html_path=persist_density_boma_urls,
        config={
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 40000,
            "max_concurrent_pages": 1,
        },
        **convert_density_boma_map_png_params,
    )
    .call()
)


# %% [markdown]
# ## Remove Geographic Outliers from Events

# %%
# parameters

exclude_event_outliers_params = dict()

# %%
# call the task


exclude_event_outliers = (
    exclude_geom_outliers.set_task_instance_id("exclude_event_outliers")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(df=map_livestock_colors, z_threshold=3, **exclude_event_outliers_params)
    .call()
)


# %% [markdown]
# ## Remove invalid points from events

# %%
# parameters

remove_invalid_geoms_params = dict()

# %%
# call the task


remove_invalid_geoms = (
    drop_null_geometry.set_task_instance_id("remove_invalid_geoms")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        gdf=exclude_event_outliers,
        geometry_column="geometry",
        **remove_invalid_geoms_params,
    )
    .call()
)


# %% [markdown]
# ## Generate livestock predation event scatterplot layer

# %%
# parameters

generate_livestock_layer_params = dict()

# %%
# call the task


generate_livestock_layer = (
    create_scatterplot_layer.set_task_instance_id("generate_livestock_layer")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        layer_style={
            "get_fill_color": "livestock_species_colors",
            "get_line_color": "livestock_species_colors",
            "get_radius": 4,
            "opacity": 0.75,
            "stroked": True,
        },
        legend={
            "title": "Livestock species",
            "label_column": "Livestock species",
            "color_column": "livestock_species_colors",
            "sort": "ascending",
            "label_suffix": None,
        },
        geodataframe=remove_invalid_geoms,
        **generate_livestock_layer_params,
    )
    .call()
)


# %% [markdown]
# ## Overlay Livestock Events on Amboseli Map

# %%
# parameters

combine_ambo_lvs_layers_params = dict()

# %%
# call the task


combine_ambo_lvs_layers = (
    combine_deckgl_map_layers.set_task_instance_id("combine_ambo_lvs_layers")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        static_layers=[create_ambo_layers_styled, create_ambo_boundaries_styled],
        grouped_layers=[generate_livestock_layer],
        **combine_ambo_lvs_layers_params,
    )
    .call()
)


# %% [markdown]
# ## Calculate Zoom Level for Livestock Event Map

# %%
# parameters

livestock_zoom_value_params = dict()

# %%
# call the task


livestock_zoom_value = (
    view_state_deck_gdf.set_task_instance_id("livestock_zoom_value")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        pitch=0, bearing=0, gdf=remove_invalid_geoms, **livestock_zoom_value_params
    )
    .call()
)


# %% [markdown]
# ## Draw livestock predation event map

# %%
# parameters

draw_livestock_map_params = dict(
    widget_id=...,
)

# %%
# call the task


draw_livestock_map = (
    draw_map.set_task_instance_id("draw_livestock_map")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        tile_layers=configure_base_maps,
        static=False,
        title=None,
        max_zoom=10,
        legend_style={"placement": "bottom-right"},
        geo_layers=combine_ambo_lvs_layers,
        view_state=livestock_zoom_value,
        **draw_livestock_map_params,
    )
    .call()
)


# %% [markdown]
# ## Persist livestock predation event map

# %%
# parameters

persist_livestock_map_params = dict(
    filename_suffix=...,
)

# %%
# call the task


persist_livestock_map = (
    persist_text.set_task_instance_id("persist_livestock_map")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        text=draw_livestock_map,
        filename="livestock_predation_event_map.html",
        **persist_livestock_map_params,
    )
    .call()
)


# %% [markdown]
# ## Convert livestock predation event map to png

# %%
# parameters

convert_livestock_map_png_params = dict()

# %%
# call the task


convert_livestock_map_png = (
    html_to_png.set_task_instance_id("convert_livestock_map_png")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        html_path=persist_livestock_map,
        config={
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 40000,
            "max_concurrent_pages": 1,
        },
        **convert_livestock_map_png_params,
    )
    .call()
)


# %% [markdown]
# ## Draw multi-line chart of total animals killed by ranch

# %%
# parameters

ranch_killed_multiline_chart_params = dict(
    widget_id=...,
)

# %%
# call the task


ranch_killed_multiline_chart = (
    draw_custom_multi_line_time_series.set_task_instance_id(
        "ranch_killed_multiline_chart"
    )
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        dataframe=map_livestock_colors,
        x_axis="time",
        y_axis="Total animals killed",
        group_column="Ranch",
        agg_function="sum",
        color_column="ranch_colors",
        line_style={"shape": "linear", "dash": "solid"},
        fill=False,
        ascending=True,
        plot_style=None,
        group_order=["Eselengei", "Mbirikani", "Kimana"],
        time_frequency=time_frequency,
        layout_style={
            "font_size": 15,
            "legend_title": "Ranch",
            "font_style": "normal",
            "font_color": "#222222",
            "showlegend": True,
            "hovermode": "x unified",
            "plot_bgcolor": "#f5f5f5",
            "title_x": 0.5,
            "xaxis": {
                "title": "Date",
                "tickangle": 45,
                "showgrid": True,
                "gridcolor": "#e5e5e5",
            },
            "yaxis": {
                "title": "Number of livestock killed",
                "showgrid": True,
                "gridcolor": "#e5e5e5",
            },
        },
        **ranch_killed_multiline_chart_params,
    )
    .call()
)


# %% [markdown]
# ## Persist multi-line chart of total animals killed by ranch

# %%
# parameters

persist_ranch_killed_multiline_params = dict()

# %%
# call the task


persist_ranch_killed_multiline = (
    persist_text.set_task_instance_id("persist_ranch_killed_multiline")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        text=ranch_killed_multiline_chart,
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename="livestock_killed_over_time_by_ranch_chart.html",
        filename_suffix=None,
        **persist_ranch_killed_multiline_params,
    )
    .call()
)


# %% [markdown]
# ## Convert multi-line chart of total animals killed by ranch to png

# %%
# parameters

convert_ranch_killed_multi_png_params = dict()

# %%
# call the task


convert_ranch_killed_multi_png = (
    html_to_png.set_task_instance_id("convert_ranch_killed_multi_png")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        html_path=persist_ranch_killed_multiline,
        config={
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 50,
            "max_concurrent_pages": 1,
        },
        **convert_ranch_killed_multi_png_params,
    )
    .call()
)


# %% [markdown]
# ## Draw multi-line chart of total animals killed by location of attack

# %%
# parameters

location_killed_multiline_chart_params = dict(
    widget_id=...,
)

# %%
# call the task


location_killed_multiline_chart = (
    draw_custom_multi_line_time_series.set_task_instance_id(
        "location_killed_multiline_chart"
    )
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        dataframe=map_livestock_colors,
        x_axis="time",
        y_axis="Total animals killed",
        group_column="Where were the livestock when the attack happened",
        agg_function="sum",
        color_column="livestock_location_colors",
        line_style={"shape": "linear", "dash": "solid"},
        fill=True,
        plot_style=None,
        group_order=["Inside Boma", "Within 200m of Boma", "More than 200m from Boma"],
        ascending=True,
        time_frequency=time_frequency,
        layout_style={
            "font_size": 15,
            "legend_title": "Location of attack",
            "font_style": "normal",
            "font_color": "#222222",
            "showlegend": True,
            "hovermode": "x unified",
            "plot_bgcolor": "#f5f5f5",
            "title_x": 0.5,
            "xaxis": {
                "title": "Date",
                "tickangle": 45,
                "showgrid": True,
                "gridcolor": "#e5e5e5",
            },
            "yaxis": {
                "title": "Number of livestock killed",
                "showgrid": True,
                "gridcolor": "#e5e5e5",
            },
        },
        **location_killed_multiline_chart_params,
    )
    .call()
)


# %% [markdown]
# ## Persist multi-line chart of total animals killed by location of attack

# %%
# parameters

persist_loc_killed_multiline_params = dict()

# %%
# call the task


persist_loc_killed_multiline = (
    persist_text.set_task_instance_id("persist_loc_killed_multiline")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        text=location_killed_multiline_chart,
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename="livestock_killed_over_time_by_attack_location_chart.html",
        filename_suffix=None,
        **persist_loc_killed_multiline_params,
    )
    .call()
)


# %% [markdown]
# ## Convert multi-line chart of total animals killed by location of attack to png

# %%
# parameters

convert_loc_killed_multiline_png_params = dict()

# %%
# call the task


convert_loc_killed_multiline_png = (
    html_to_png.set_task_instance_id("convert_loc_killed_multiline_png")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        html_path=persist_loc_killed_multiline,
        config={
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 50,
            "max_concurrent_pages": 1,
        },
        **convert_loc_killed_multiline_png_params,
    )
    .call()
)


# %% [markdown]
# ## Draw multi-line chart of incidents by type of claim

# %%
# parameters

claim_type_multiline_chart_params = dict(
    widget_id=...,
)

# %%
# call the task


claim_type_multiline_chart = (
    draw_custom_multi_line_time_series.set_task_instance_id(
        "claim_type_multiline_chart"
    )
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        dataframe=map_livestock_colors,
        x_axis="time",
        y_axis="id",
        group_column="Type of claim",
        agg_function="count",
        color_column="type_of_claim_colors",
        line_style={"shape": "linear", "dash": "solid"},
        fill=True,
        plot_style=None,
        ascending=True,
        group_order=["Lost in the Bush", "No Penalty", "Bad Boma"],
        time_frequency=time_frequency,
        layout_style={
            "font_size": 15,
            "legend_title": "Type of claim",
            "font_style": "normal",
            "font_color": "#222222",
            "showlegend": True,
            "hovermode": "x unified",
            "plot_bgcolor": "#f5f5f5",
            "title_x": 0.5,
            "xaxis": {
                "title": "Date",
                "tickangle": 45,
                "showgrid": True,
                "gridcolor": "#e5e5e5",
            },
            "yaxis": {
                "title": "Number of claims",
                "showgrid": True,
                "gridcolor": "#e5e5e5",
            },
        },
        **claim_type_multiline_chart_params,
    )
    .call()
)


# %% [markdown]
# ## Persist multi-line chart of incidents by type of claim

# %%
# parameters

persist_claim_type_multiline_params = dict()

# %%
# call the task


persist_claim_type_multiline = (
    persist_text.set_task_instance_id("persist_claim_type_multiline")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        text=claim_type_multiline_chart,
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename="claim_count_over_time_by_type_chart.html",
        filename_suffix=None,
        **persist_claim_type_multiline_params,
    )
    .call()
)


# %% [markdown]
# ## Convert multi-line chart of incidents by type of claim to png

# %%
# parameters

convert_claim_type_multiline_png_params = dict()

# %%
# call the task


convert_claim_type_multiline_png = (
    html_to_png.set_task_instance_id("convert_claim_type_multiline_png")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        html_path=persist_claim_type_multiline,
        config={
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 50,
            "max_concurrent_pages": 1,
        },
        **convert_claim_type_multiline_png_params,
    )
    .call()
)


# %% [markdown]
# ## Draw multi-bar time series chart of animals killed by predator species

# %%
# parameters

predator_killed_multibar_chart_params = dict(
    widget_id=...,
)

# %%
# call the task


predator_killed_multibar_chart = (
    draw_custom_multi_bar_time_series.set_task_instance_id(
        "predator_killed_multibar_chart"
    )
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        dataframe=map_livestock_colors,
        x_axis="time",
        y_axis="Total animals killed",
        group_column="Animal responsible",
        agg_function="sum",
        time_frequency=time_frequency,
        ncols=2,
        shared_yaxes=False,
        group_order=None,
        row_height=350,
        ascending=True,
        bar_color="#6495ed",
        x_axis_style={"title": "Date"},
        y_axis_style={"title": "Total animals killed"},
        tick_font_size=None,
        plot_style=None,
        layout_style=None,
        **predator_killed_multibar_chart_params,
    )
    .call()
)


# %% [markdown]
# ## Persist multi-bar time series chart of animals killed by predator species

# %%
# parameters

persist_predator_killed_multibar_params = dict()

# %%
# call the task


persist_predator_killed_multibar = (
    persist_text.set_task_instance_id("persist_predator_killed_multibar")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        text=predator_killed_multibar_chart,
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename="livestock_killed_over_time_by_predator_mulit_bar_chart.html",
        filename_suffix=None,
        **persist_predator_killed_multibar_params,
    )
    .call()
)


# %% [markdown]
# ## Convert multi-line time series chart of incidents by type of claim to png

# %%
# parameters

convert_pred_killed_multibar_png_params = dict()

# %%
# call the task


convert_pred_killed_multibar_png = (
    html_to_png.set_task_instance_id("convert_pred_killed_multibar_png")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        html_path=persist_predator_killed_multibar,
        config={
            "width": 1280,
            "height": 2000,
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 50,
            "max_concurrent_pages": 1,
        },
        **convert_pred_killed_multibar_png_params,
    )
    .call()
)


# %% [markdown]
# ## Group current ranch level groupers and previous ranch level groupers

# %%
# parameters

group_ranch_level_groupers_params = dict()

# %%
# call the task


group_ranch_level_groupers = (
    zip_groupbykey.set_task_instance_id("group_ranch_level_groupers")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[split_by_ranch, split_previous_by_ranch],
        **group_ranch_level_groupers_params,
    )
    .call()
)


# %% [markdown]
# ## Draw historic time series chart

# %%
# parameters

draw_ranch_level_historic_chart_params = dict(
    widget_id=...,
)

# %%
# call the task


draw_ranch_level_historic_chart = (
    draw_historic_time_series_chart.set_task_instance_id(
        "draw_ranch_level_historic_chart"
    )
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        x_axis="time",
        y_axis="Total animals killed",
        agg_function="sum",
        time_frequency=time_frequency,
        ncols=2,
        shared_yaxes=False,
        row_height=350,
        group_order=None,
        group_column="Animal responsible",
        ascending=True,
        x_axis_style={"title": "Date"},
        y_axis_style={"title": "Total animals killed"},
        current_line_color="#6495ed",
        historical_line_color="#ff7f0e",
        ci_fill_color=None,
        ci_multiplier=1.96,
        ci_method="seasonal",
        current_label="Actual",
        historical_label="Historic Mean",
        ci_label="95% Confidence Interval",
        layout_style=None,
        tick_font_size=None,
        **draw_ranch_level_historic_chart_params,
    )
    .mapvalues(
        argnames=["current_dataframe", "previous_dataframe"],
        argvalues=group_ranch_level_groupers,
    )
)


# %% [markdown]
# ## Add Ranch Name to Historic chart

# %%
# parameters

combine_ranch_name_chart_params = dict()

# %%
# call the task


combine_ranch_name_chart = (
    zip_groupbykey.set_task_instance_id("combine_ranch_name_chart")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[get_ranch_name, draw_ranch_level_historic_chart],
        **combine_ranch_name_chart_params,
    )
    .call()
)


# %% [markdown]
# ## Persist historic time series chart

# %%
# parameters

persist_ranch_historic_chart_params = dict()

# %%
# call the task


persist_ranch_historic_chart = (
    persist_text.set_task_instance_id("persist_ranch_historic_chart")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename="ranch_level_historic_time_series_chart.html",
        **persist_ranch_historic_chart_params,
    )
    .mapvalues(argnames=["filename_suffix", "text"], argvalues=combine_ranch_name_chart)
)


# %% [markdown]
# ## Convert multi-line time series chart of incidents by type of claim to png

# %%
# parameters

convert_ranch_historic_chart_png_params = dict()

# %%
# call the task


convert_ranch_historic_chart_png = (
    html_to_png.set_task_instance_id("convert_ranch_historic_chart_png")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        config={
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 50,
            "max_concurrent_pages": 1,
            "width": 1280,
            "height": 2000,
        },
        **convert_ranch_historic_chart_png_params,
    )
    .mapvalues(argnames=["html_path"], argvalues=persist_ranch_historic_chart)
)


# %% [markdown]
# ## Download template for Big Life PCF Dashboard Report

# %%
# parameters

download_big_life_template_params = dict()

# %%
# call the task


download_big_life_template = (
    fetch_and_persist_file.set_task_instance_id("download_big_life_template")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        url="https://www.dropbox.com/scl/fi/i1v1emk1s2sb15w2arhld/pcf_report_template.docx?rlkey=16850ymbagt9vbuyqn8fsg7tc&st=zuxci7sv&dl=0",
        output_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        overwrite_existing=False,
        retries=3,
        unzip=False,
        **download_big_life_template_params,
    )
    .call()
)


# %% [markdown]
# ## Get user name to use on template

# %%
# parameters

get_user_name_params = dict()

# %%
# call the task


get_user_name = (
    get_current_user.set_task_instance_id("get_user_name")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(client=er_client_name, **get_user_name_params)
    .call()
)


# %% [markdown]
# ## Get user full name

# %%
# parameters

get_fullname_params = dict()

# %%
# call the task


get_fullname = (
    get_user_full_name.set_task_instance_id("get_fullname")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(user=get_user_name, **get_fullname_params)
    .call()
)


# %% [markdown]
# ## Add outputs generated to template and persist

# %%
# parameters

generate_big_life_report_params = dict()

# %%
# call the task


generate_big_life_report = (
    generate_pcf_report.set_task_instance_id("generate_big_life_report")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        template_path=download_big_life_template,
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename="big_life_pcf_report.docx",
        time_period=time_range,
        generated_by=get_fullname,
        validate_images=True,
        **generate_big_life_report_params,
    )
    .call()
)


# %% [markdown]
# ## Generate Big Life PCF Dashboard Report

# %%
# parameters

big_life_pcf_dashboard_report_params = dict(
    warning=...,
)

# %%
# call the task


big_life_pcf_dashboard_report = (
    gather_dashboard.set_task_instance_id("big_life_pcf_dashboard_report")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        details=workflow_details,
        widgets=[],
        time_range=time_range,
        groupers=groupers,
        **big_life_pcf_dashboard_report_params,
    )
    .call()
)
